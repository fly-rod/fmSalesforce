# Salesforce Credentials
# Option 1: Username/Password flow (simpler, good for scripts)
SF_USERNAME=your_username@example.com
SF_PASSWORD=your_password
SF_SECURITY_TOKEN=your_security_token
SF_DOMAIN=login  # Use 'test' for sandbox, 'login' for production

# Option 2: Use SF CLI session (recommended if already authenticated)
# Set this to 'true' to use the SF CLI session instead of username/password
SF_USE_CLI_SESSION=false
SF_CLI_TARGET_ORG=FMCDev  # Alias of your SF org

# Claude API
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# Output Configuration
OUTPUT_DIR=./output
REPORT_FILENAME=crm_summary_report.pdf
